import { CommonModule } from '@angular/common';import { Component,OnInit } from '@angular/core';import { FormsModule } from '@angular/forms';import { TicketsService } from './tickets.service';import { Modal,PageHeader } from '../../shared/ui';
@Component({standalone:true,imports:[CommonModule,FormsModule,Modal,PageHeader],template:`<qai-page-header title="Tickets & SLA" subtitle="Run support operations with assignment, priority, SLA and resolution tracking."><button (click)="load()">↻ Refresh</button><button class="primary" (click)="show=true">+ Create ticket</button></qai-page-header><div class="toolbar"><input [(ngModel)]="q" placeholder="Search tickets"><select [(ngModel)]="status"><option value="">All statuses</option><option>New</option><option>Open</option><option>Pending</option><option>Resolved</option></select></div><section class="panel table-wrap"><table><thead><tr><th>Ticket</th><th>Subject</th><th>Priority</th><th>Status</th><th>SLA</th><th>Created</th><th></th></tr></thead><tbody><tr *ngFor="let t of visible" (click)="edit(t)"><td><b>{{t.number}}</b></td><td>{{t.subject}}</td><td><span class="pill" [class.hot]="isHigh(t)">{{label(t.priority)}}</span></td><td>{{label(t.status)}}</td><td><span class="sla">{{sla(t)}}</span></td><td>{{t.createdAtUtc|date:'short'}}</td><td><button class="small" (click)="edit(t);$event.stopPropagation()">Open</button></td></tr></tbody></table></section><qai-modal [open]="show" title="Create support ticket" (close)="show=false"><form class="form" (ngSubmit)="save()"><label>Subject<input [(ngModel)]="form.subject" name="subject" required></label><label>Description<textarea [(ngModel)]="form.description" name="description"></textarea></label><div class="form2"><label>Priority<select [(ngModel)]="form.priority" name="priority"><option [ngValue]="0">Low</option><option [ngValue]="1">Normal</option><option [ngValue]="2">High</option><option [ngValue]="3">Urgent</option></select></label><label>Status<select [(ngModel)]="form.status" name="status"><option [ngValue]="0">New</option><option [ngValue]="1">Open</option><option [ngValue]="2">Pending</option></select></label></div><footer><button type="button" (click)="show=false">Cancel</button><button class="primary" type="submit">Create ticket</button></footer></form></qai-modal>`})export class TicketsPage implements OnInit{rows:any[]=[];q='';status='';show=false;form:any={subject:'',description:'',priority:1,status:0};constructor(private data:TicketsService){}ngOnInit(){this.load()}load(){this.data.list<any[]>().subscribe(r=>this.rows=r)}get visible(){return this.rows.filter(x=>(!this.q||(x.number+' '+x.subject).toLowerCase().includes(this.q.toLowerCase()))&&(!this.status||this.label(x.status)===this.status))}save(){(this.form.id?this.data.update<any>(this.form.id,this.form):this.data.create<any>(this.form)).subscribe(r=>{this.rows.unshift(r);this.show=false;this.form={subject:'',description:'',priority:1,status:0}})}edit(t:any){this.form={...t};this.show=true}label(v:any){if(typeof v==='string')return v;return ['New','Open','Pending','Resolved','Closed'][v]||['Low','Normal','High','Urgent'][v]||String(v)}isHigh(t:any){return t.priority===2||t.priority===3||String(t.priority).match(/High|Urgent/)}sla(t:any){return t.resolutionDueUtc?'Due '+new Date(t.resolutionDueUtc).toLocaleString():'Within SLA'}}