import { CommonModule } from '@angular/common';import { Component,OnInit } from '@angular/core';import { Router } from '@angular/router';import { DashboardService } from './dashboard.service';import { PageHeader } from '../../shared/ui';
@Component({standalone:true,imports:[CommonModule,PageHeader],template:`<qai-page-header title="Revenue Command Center" subtitle="Automated support, sales qualification and revenue automation in one operating view."><button (click)="refresh()">↻ Refresh</button><button class="primary" (click)="go('/automations')">Review automations</button></qai-page-header><div class="metrics"><article (click)="go('/crm/leads')"><span>Qualified leads</span><strong>{{d.leads||0}}</strong><small>Persisted CRM leads</small></article><article (click)="go('/crm/leads')"><span>Hot leads</span><strong>{{d.hotLeads||0}}</strong><small>Needs sales action</small></article><article (click)="go('/pipeline')"><span>Open pipeline</span><strong>{{money(d.pipeline||0)}}</strong><small>Current SQL pipeline</small></article><article (click)="go('/inbox')"><span>Open conversations</span><strong>{{d.openConversations||0}}</strong><small>Customer conversations</small></article><article (click)="go('/tickets')"><span>Open tickets</span><strong>{{d.openTickets||0}}</strong><small>Requires resolution</small></article></div><div class="grid2"><section class="panel"><header><div><b>Revenue influenced</b><span>Persisted attribution and pipeline</span></div><strong>{{money(d.influencedRevenue||0)}}</strong></header><footer><span>Open pipeline <b>{{money(d.pipeline||0)}}</b></span><span>Won revenue <b>{{money(d.wonRevenue||0)}}</b></span></footer></section><section class="panel"><header><div><b>Automation impact</b><span>Measured from completed database records</span></div></header><div class="impact"><div><b>{{d.estimatedHoursSaved||0}}h</b><span>Estimated time saved</span></div><div><b>{{d.automationActions||0}}</b><span>Actions executed</span></div><div><b>{{d.meetingsBooked||0}}</b><span>Meetings booked</span></div><div><b>{{d.completedRuns||0}}</b><span>Completed runs</span></div></div><button class="link" (click)="go('/automations')">Review automations →</button></section></div><div class="grid2"><section class="panel"><header><b>Priority opportunities</b><button (click)="go('/pipeline')">View pipeline</button></header><table><thead><tr><th>Company</th><th>Intent</th><th>Score</th><th>Value</th><th>Next action</th></tr></thead><tbody><tr *ngFor="let x of d.opportunities||[]"><td><b>{{x.company}}</b><small>{{x.country}}</small></td><td>{{x.intent}}</td><td><span class="score hot">{{x.score}}</span></td><td>{{money(x.value)}}</td><td><button class="small" (click)="go('/pipeline')">Open</button></td></tr></tbody></table><p *ngIf="!d.opportunities?.length">No persisted opportunities yet.</p></section><section class="panel"><header><b>Knowledge gaps</b><button (click)="go('/knowledge/gaps')">Resolve</button></header><div class="gap" *ngFor="let g of d.knowledgeGaps||[]"><div><b>{{g.topic}}</b><span>{{g.count}} unanswered questions</span></div><em>{{g.impact}}</em></div><p *ngIf="!d.knowledgeGaps?.length">No open knowledge gaps.</p></section></div>`})export class DashboardPage implements OnInit{d:any={};constructor(private data:DashboardService,private router:Router){}ngOnInit(){this.refresh()}refresh(){this.data.summary<any>().subscribe({next:r=>this.d=r,error:()=>{}})}go(x:string){this.router.navigateByUrl(x)}money(v:number){return new Intl.NumberFormat('de-DE',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(v||0)}}
