import { CommonModule } from '@angular/common';import { Component,OnInit } from '@angular/core';import { FormsModule } from '@angular/forms';import { Modal,PageHeader } from '../../shared/ui';import { MeetingsService } from './meetings.service';
@Component({standalone:true,imports:[CommonModule,FormsModule,Modal,PageHeader],template:`<qai-page-header title="Meetings" subtitle="AI-booked demos and sales calls with calendar-ready data."><button (click)="load()">↻ Refresh</button><button class="primary" (click)="open()">+ Book meeting</button></qai-page-header><section class="panel"><div class="meeting" *ngFor="let x of rows"><time>{{x.startsAtUtc|date:'HH:mm'}}</time><i></i><div><b>Sales meeting</b><span>{{x.startsAtUtc|date:'fullDate'}} · {{duration(x)}} min</span></div><span class="pill">{{x.status}}</span><button (click)="open(x)">Edit</button><button class="danger" (click)="remove(x)">Cancel</button></div></section><qai-modal [open]="show" [title]="form.id?'Edit meeting':'Book sales meeting'" (close)="show=false"><form class="form" (ngSubmit)="save()"><label>Meeting type ID<input [(ngModel)]="form.meetingTypeId" name="type" required></label><div class="form2"><label>Date<input type="date" [(ngModel)]="date" name="date"></label><label>Time<input type="time" [(ngModel)]="time" name="time"></label></div><label>Status<select [(ngModel)]="form.status" name="status"><option>booked</option><option>completed</option><option>cancelled</option><option>no-show</option></select></label><footer><button type="button" (click)="show=false">Close</button><button class="primary" type="submit">Save & notify</button></footer></form></qai-modal>`})
export class MeetingsPage implements OnInit{rows:any[]=[];show=false;form:any={status:'booked'};date='';time='09:00';constructor(private data:MeetingsService){}ngOnInit(){this.load()}load(){this.data.list().subscribe(r=>this.rows=r)}open(x?:any){this.form=x?{...x}:{status:'booked'};const d=x?.startsAtUtc?new Date(x.startsAtUtc):new Date();this.date=d.toISOString().slice(0,10);this.time=d.toTimeString().slice(0,5);this.show=true}save(){const start=new Date(`${this.date}T${this.time}:00`);this.form.startsAtUtc=start.toISOString();this.form.endsAtUtc=new Date(start.getTime()+30*60000).toISOString();const op=this.form.id?this.data.update(this.form.id,this.form):this.data.create(this.form);op.subscribe(r=>{const i=this.rows.findIndex(x=>x.id===r.id);i>=0?this.rows[i]=r:this.rows.unshift(r);this.show=false})}remove(x:any){if(confirm('Cancel this meeting?'))this.data.remove(x.id).subscribe(()=>this.rows=this.rows.filter(v=>v.id!==x.id))}duration(x:any){return Math.max(0,Math.round((new Date(x.endsAtUtc).getTime()-new Date(x.startsAtUtc).getTime())/60000))}}
