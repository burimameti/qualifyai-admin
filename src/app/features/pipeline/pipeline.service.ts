import { Injectable } from '@angular/core';import { ApiService } from '../../core/api.service';import { Opportunity } from '../../core/models/platform.models';
@Injectable({providedIn:'root'}) export class PipelineService{constructor(private api:ApiService){}load(){return this.api.get<any>('sales/pipelines')}opportunities(){return this.api.get<Opportunity[]>('crm/opportunities')}move(id:string,stageId:string){return this.api.put<Opportunity>(`crm/opportunities/${id}/stage`,{stageId})}}
