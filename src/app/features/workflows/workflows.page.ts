import { CommonModule } from '@angular/common';import { Component,OnInit } from '@angular/core';import { FormsModule } from '@angular/forms';import { Modal,PageHeader } from '../../shared/ui';import { WorkflowsService } from './workflows.service';
@Component({standalone:true,imports:[CommonModule,FormsModule,Modal,PageHeader],template:`<qai-page-header title="Qualification Workflows" subtitle="Design how QualifyAI asks, branches, scores and executes revenue actions."><select [(ngModel)]="activeId" (change)="loadDesigner()"><option *ngFor="let f of flows" [value]="f.id">{{f.name}}</option></select><button (click)="newOpen=true">+ Workflow</button><button class="primary" (click)="save()">Save workflow</button></qai-page-header><div class="workflow"><aside><b>NODE LIBRARY</b><button *ngFor="let n of library" (click)="add(n.type)"><span>{{n.icon}}</span>{{n.label}}</button><hr><button (click)="connectSelected()" [disabled]="!selected">Connect selected → last node</button></aside><section class="canvas"><div class="node" *ngFor="let n of nodes" [style.left.px]="n.x" [style.top.px]="n.y" (click)="selected=n"><small>{{n.type|uppercase}}</small><b>{{nodeTitle(n)}}</b><span>{{nodeText(n)}}</span><i></i></div><svg><line *ngFor="let e of edges;let i=index" [attr.x1]="edgePoint(e,true,'x')" [attr.y1]="edgePoint(e,true,'y')" [attr.x2]="edgePoint(e,false,'x')" [attr.y2]="edgePoint(e,false,'y')" stroke="#94a3b8" stroke-width="2"/></svg></section><aside class="inspector" *ngIf="selected"><h3>Node settings</h3><label>Type<input [(ngModel)]="selected.type"></label><label>Configuration<textarea [(ngModel)]="selected.configJson"></textarea></label><div class="form2"><label>X<input type="number" [(ngModel)]="selected.x"></label><label>Y<input type="number" [(ngModel)]="selected.y"></label></div><button class="danger" (click)="remove(selected)">Delete node</button></aside></div><qai-modal [open]="newOpen" title="New qualification workflow" (close)="newOpen=false"><form class="form" (ngSubmit)="createFlow()"><label>Name<input [(ngModel)]="newName" name="name" required></label><footer><button type="button" (click)="newOpen=false">Cancel</button><button class="primary" type="submit">Create</button></footer></form></qai-modal>`})
export class WorkflowsPage implements OnInit{flows:any[]=[];activeId='';nodes:any[]=[];edges:any[]=[];selected:any;newOpen=false;newName='';library=[{type:'question',label:'Question',icon:'?'},{type:'condition',label:'Condition',icon:'◇'},{type:'score',label:'Lead score',icon:'+'},{type:'action',label:'Action',icon:'⚡'},{type:'meeting',label:'Book meeting',icon:'◷'},{type:'handoff',label:'Human handoff',icon:'↗'}];constructor(private data:WorkflowsService){}ngOnInit(){this.load()}load(){this.data.list().subscribe(r=>{this.flows=r;if(r.length&&!this.activeId){this.activeId=r[0].id;this.loadDesigner()}})}createFlow(){this.data.create({name:this.newName,active:true}).subscribe(r=>{this.flows.push(r);this.activeId=r.id;this.nodes=[];this.edges=[];this.newName='';this.newOpen=false})}loadDesigner(){if(!this.activeId)return;this.data.designer(this.activeId).subscribe(r=>{this.nodes=r.nodes||[];this.edges=r.edges||[]})}add(type:string){const n={id:crypto.randomUUID(),flowId:this.activeId,nodeKey:'node_'+Date.now(),type,configJson:'{}',x:180+(this.nodes.length%4)*220,y:100+Math.floor(this.nodes.length/4)*150};this.nodes.push(n);this.selected=n}remove(n:any){this.nodes=this.nodes.filter(x=>x!==n);this.edges=this.edges.filter(e=>e.fromNodeKey!==n.nodeKey&&e.toNodeKey!==n.nodeKey);this.selected=null}connectSelected(){if(!this.selected||this.nodes.length<2)return;let target:any=null;for(let i=this.nodes.length-1;i>=0;i--){if(this.nodes[i]!==this.selected){target=this.nodes[i];break}}if(!target)return;this.edges.push({id:crypto.randomUUID(),flowId:this.activeId,fromNodeKey:this.selected.nodeKey,toNodeKey:target.nodeKey,conditionJson:'{}'})}save(){if(!this.activeId)return;this.data.saveDesigner(this.activeId,{nodes:this.nodes,edges:this.edges}).subscribe(()=>alert('Workflow saved.'))}nodeTitle(n:any){return ({question:'Qualification question',condition:'Branch condition',score:'Adjust lead score',action:'Execute business action',meeting:'Book sales meeting',handoff:'Assign human agent'} as any)[n.type]||n.type}nodeText(n:any){try{const c=JSON.parse(n.configJson||'{}');return c.question||c.action||c.condition||'Configure this node'}catch{return 'Configure this node'}}edgePoint(e:any,from:boolean,axis:'x'|'y'){const n=this.nodes.find(x=>x.nodeKey===(from?e.fromNodeKey:e.toNodeKey));return axis==='x'?(n?.x||0)+140:(n?.y||0)+50}}
